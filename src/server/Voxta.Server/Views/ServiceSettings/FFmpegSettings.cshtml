@model Voxta.Server.ViewModels.ServiceSettings.FFmpegSettingsViewModel
@{
    Layout = "_Layout";
    ViewBag.Title = "Settings - FFmpeg";
}

<div class="container">
    <h1>FFmpeg Settings</h1>
    
    <div class="card mb-3">
        <div class="card-header">
            <h2>Help</h2>
        </div>
        <div class="card-body">
            <p>This uses the FFmpeg tools for recording and audio conversion.</p>
        </div>
    </div>
    
    <form id="configureForm" method="post" asp-action="PostFFmpegSettings">
        <div class="card mb-3">
            <div class="card-header">
                <h2>Configuration</h2>
            </div>
            <div class="card-body">
                <div class="form-check mb-3">
                    <input type="checkbox" asp-for="Enabled" class="form-check-input"/>
                    <label asp-for="Enabled">Enabled</label>
                    <div class="form-text">You can disable this service if you don't want it to be selected automatically.</div>
                    <span asp-validation-for="Enabled" class="text-danger"></span>
                </div>
            </div>
        </div>

        <partial name="ServiceSettings/_FormFooter" model="@Url.Action("DeleteFFmpegSettings")" />
    </form>
</div>
